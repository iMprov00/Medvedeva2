<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <meta name="format-detection" content="telephone=no">
    <title>Клиника доказательной медицины доктора Медведевой</title>
    
    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <!-- Наши CSS файлы -->
    <link rel="stylesheet" href="/css/main.css">
    <link rel="stylesheet" href="/css/components.css">
    
    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
    
    <!-- Шрифты -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    
    <!-- PhotoSwipe CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/photoswipe@5.4.4/dist/photoswipe.min.css">
    

    
    <!-- Мета-теги -->
    <meta name="description" content="Клиника доказательной медицины доктора Медведевой. Современный подход к лечению, основанный на научных данных и индивидуальном подходе к каждому пациенту.">
    <meta name="keywords" content="клиника, доказательная медицина, врач, лечение, здоровье">
    <meta property="og:title" content="Клиника доказательной медицины доктора Медведевой">
    <meta property="og:description" content="Современный подход к лечению, основанный на научных данных">
    <meta property="og:type" content="website">
    <meta property="og:url" content="https://medvedeva-clinic.ru">
    
    <!-- Фавиконки -->
    <link rel="icon" href="/favicon.ico" type="image/x-icon">
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png">
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png">
    
    <!-- Канонический URL -->
    <link rel="canonical" href="https://medvedeva-clinic.ru">
    
    <!-- Для мобильных устройств -->
    <meta name="theme-color" content="#7effcd">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
    <meta name="apple-mobile-web-app-title" content="Клиника Медведевой">
    
    <!-- Стили для виджета MedFlex -->
    <style>
        #medflexMedtochkaWidgetButton {
            display: inline-block !important;
        }
        
        /* Стилизация кнопки виджета */
        #medflexMedtochkaWidgetButton button {
            background-color: #0d6efd !important;
            border-color: #0d6efd !important;
            border-radius: 0.375rem !important;
            padding: 0.375rem 0.75rem !important;
            font-size: 0.875rem !important;
            color: white !important;
            font-weight: 500 !important;
            transition: all 0.2s ease-in-out !important;
            min-width: 120px !important;
        }
        
        #medflexMedtochkaWidgetButton button:hover {
            background-color: #0b5ed7 !important;
            border-color: #0a58ca !important;
            transform: translateY(-1px) !important;
            box-shadow: 0 4px 8px rgba(0,0,0,0.1) !important;
        }
        
        #medflexMedtochkaWidgetButton button:active {
            transform: translateY(0) !important;
        }
        
        /* Иконка на кнопке */
        #medflexMedtochkaWidgetButton button i,
        #medflexMedtochkaWidgetButton button svg {
            margin-right: 5px !important;
        }
    </style>
</head>
<body class="safe-area-top safe-area-bottom">
    <% unless request.path.start_with?('/admin') %>
    <!-- Шапка -->
<!-- Шапка -->
<header class="header sticky-top bg-white shadow-sm">
    <nav class="navbar navbar-expand-lg navbar-light py-2 py-lg-3">
        <div class="container-fluid container-lg">
            <!-- Логотип и название -->
            <a class="navbar-brand d-flex align-items-center me-auto" href="/">
                <div class="me-2 me-lg-3">
                    <img src="/images/logo.png" 
                         alt="Логотип клиники доктора Медведевой" 
                         height="50" width="50" 
                         class="img-fluid d-none d-md-block"
                         loading="eager">
                    <!-- Уменьшенный логотип для мобильных -->
                    <img src="/images/logo.png" 
                         alt="Логотип клиники доктора Медведевой" 
                         height="40" width="40" 
                         class="img-fluid d-md-none"
                         loading="eager">
                </div>
                <div class="d-none d-md-block">
                    <span class="fw-bold fs-6 fs-lg-5 text-dark">Клиника доктора Медведевой</span>
                    <small class="text-muted d-block d-lg-inline d-lg-block">Доказательная медицина</small>
                </div>
                <!-- Уменьшенный текст для мобильных -->
                <div class="d-md-none">
                    <span class="fw-bold text-dark" style="font-size: 0.9rem; line-height: 1.2;">
                        Клиника доктора<br>Медведевой
                    </span>
                </div>
            </a>
            
            <!-- Убираем кнопку телефона из мобильного хедера -->
            
            <!-- Бургер-меню -->
            <button class="navbar-toggler touch-target ms-2" 
                    type="button" 
                    data-bs-toggle="collapse" 
                    data-bs-target="#navbarNav"
                    aria-label="Меню"
                    aria-controls="navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            
            <!-- Содержимое меню -->
            <div class="collapse navbar-collapse" id="navbarNav">
                <!-- Личный кабинет в начале для мобильных -->
                <div class="d-lg-none mb-3">
                    <div id="medflexMedtochkaWidgetButtonMobile" 
                         data-src="https://booking.medflex.ru/?user=83de76a588b26ad9b729c20d4b1ffbf6"
                         class="d-block mb-3"></div>
                </div>
                
                <!-- Основное меню -->
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="nav-item">
                        <a class="nav-link py-2 py-lg-3 <%= 'active fw-bold' if request.path == '/' %>" 
                           href="/">Главная</a>
                    </li>
                    
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle py-2 py-lg-3 <%= 'active fw-bold' if request.path == '/about' %>" 
                           href="#" 
                           role="button" 
                           data-bs-toggle="dropdown" 
                           aria-expanded="false">
                            О клинике
                        </a>
                        <ul class="dropdown-menu">
                            <li><a class="dropdown-item py-2 <%= 'active' if request.path == '/about' %>" href="/about">О нас</a></li>
                            <li><a class="dropdown-item py-2" href="/about#founder">Основатель</a></li>
                            <li><a class="dropdown-item py-2" href="/about#gallery">Галерея</a></li>
                            <li><a class="dropdown-item py-2" href="/about#reviews">Отзывы</a></li>
                        </ul>
                    </li>
                    
                    <li class="nav-item">
                        <a class="nav-link py-2 py-lg-3 <%= 'active fw-bold' if request.path == '/doctors' %>" 
                           href="/doctors">Врачи</a>
                    </li>
                    
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle py-2 py-lg-3 <%= 'active fw-bold' if request.path == '/prices' %>" 
                           href="#" 
                           role="button" 
                           data-bs-toggle="dropdown" 
                           aria-expanded="false">
                            Услуги и цены
                        </a>
                        <ul class="dropdown-menu">
                            <li><a class="dropdown-item py-2 <%= 'active' if request.path == '/prices' %>" href="/prices">Прайс-лист</a></li>
                            <li><a class="dropdown-item py-2" href="/prices#promotions">Акции</a></li>
                        </ul>
                    </li>
                    
                    <li class="nav-item">
                        <a class="nav-link py-2 py-lg-3 <%= 'active fw-bold' if request.path == '/contacts' %>" 
                           href="/contacts">Контакты</a>
                    </li>
                </ul>
                
                <!-- Телефоны для десктопа -->
                <div class="d-none d-lg-flex align-items-center ms-lg-4">
                    <div class="me-4">
                        <div class="d-flex flex-column">
                            <a href="tel:+79133650464" class="text-decoration-none text-dark fw-semibold small mb-1">
                                <i class="bi bi-telephone me-1"></i>+7 (913) 365-04-64
                            </a>
                            <a href="tel:+73852256575" class="text-decoration-none text-dark fw-semibold small">
                                <i class="bi bi-telephone me-1"></i>+7 (385) 225-65-75
                            </a>
                        </div>
                    </div>
                    <!-- Виджет Личного кабинета пациента -->
                    <div id="medflexMedtochkaWidgetButton" 
                         data-src="https://booking.medflex.ru/?user=83de76a588b26ad9b729c20d4b1ffbf6"
                         class="d-inline-block"></div>
                </div>
                
                <!-- Телефоны для мобильных (в конце меню) -->
                <div class="d-lg-none mt-3 pt-3 border-top">
                    <h6 class="fw-bold mb-2 text-muted small">Телефон:</h6>
                    <div class="mb-2">
                        <a href="tel:+79133650464" class="btn btn-outline-primary w-100 touch-target">
                            <i class="bi bi-telephone me-1"></i>+7 (913) 365-04-64
                        </a>
                    </div>
                    <div>
                        <a href="tel:+73852256575" class="btn btn-outline-primary w-100 touch-target">
                            <i class="bi bi-telephone me-1"></i>+7 (385) 225-65-75
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </nav>
</header>

    <!-- Основное содержимое -->
    <main class="min-vh-50">
        <%= yield %>
    </main>

    <!-- Подвал -->
    <footer class="bg-dark text-white pt-4 pb-3">
        <div class="container">
            <div class="row g-3">
                <div class="col-12 col-md-6 col-lg-4 order-1">
                    <a href="/" class="text-decoration-none d-flex align-items-center mb-3">
                        <div class="me-3">
                            <img src="/images/logo.png" 
                                 alt="Логотип клиники доктора Медведевой" 
                                 height="60" width="60" 
                                 class="img-fluid">
                        </div>
                        <div>
                            <span class="fw-bold fs-6 text-white">Клиника доктора Медведевой</span>
                            <small class="text-light d-block">Доказательная медицина</small>
                        </div>
                    </a>
                    <p class="text-light opacity-75 small">
                        Профессиональный подход к здоровью вашего ребенка, основанный на научных данных и современных протоколах лечения.
                    </p>
                </div>
                
                <div class="col-6 col-md-3 col-lg-2 order-3 order-md-2">
                    <h5 class="fw-bold mb-3 fs-6">Навигация</h5>
                    <ul class="list-unstyled">
                        <li class="mb-2"><a href="/" class="text-light text-decoration-none opacity-75 hover-opacity-100 small">Главная</a></li>
                        <li class="mb-2"><a href="/about" class="text-light text-decoration-none opacity-75 hover-opacity-100 small">О клинике</a></li>
                        <li class="mb-2"><a href="/doctors" class="text-light text-decoration-none opacity-75 hover-opacity-100 small">Врачи</a></li>
                        <li class="mb-2"><a href="/prices" class="text-light text-decoration-none opacity-75 hover-opacity-100 small">Услуги и цены</a></li>
                        <li class="mb-2"><a href="/contacts" class="text-light text-decoration-none opacity-75 hover-opacity-100 small">Контакты</a></li>
                    </ul>
                </div>
                
                <div class="col-6 col-md-3 col-lg-3 order-4 order-md-3">
                    <h5 class="fw-bold mb-3 fs-6">Контакты</h5>
                    <address class="text-light opacity-75 small">
                        <p class="mb-2">
                            <i class="bi bi-geo-alt me-2"></i>г. Барнаул, ул. 280-летия Барнаула, д. 22
                        </p>
                        <p class="mb-2">
                            <i class="bi bi-telephone me-2"></i>
                            <a href="tel:+79133650464" class="text-light text-decoration-none">+7 (913) 365-04-64</a>
                        </p>
                        <p class="mb-2">
                            <i class="bi bi-telephone me-2"></i>
                            <a href="tel:+73852256575" class="text-light text-decoration-none">+7 (385) 225-65-75</a>
                        </p>
                        <p class="mb-0">
                            <i class="bi bi-envelope me-2"></i>
                            <a href="mailto:info@medvedeva-clinic.ru" class="text-light text-decoration-none">info@medvedeva-clinic.ru</a>
                        </p>
                    </address>
                </div>
                
                <div class="col-12 col-md-6 col-lg-3 order-2 order-md-4">
                    <h5 class="fw-bold mb-3 fs-6">Часы работы</h5>
                    <div class="text-light opacity-75 small">
                        <p class="mb-2">Пн-Пт: 09:00 - 19:00</p>
                        <p class="mb-2">Сб: 9:15 - 15:00</p>
                        <p class="mb-3">Вс: Выходной</p>
<%#                         <!-- Дополнительно можно разместить виджет и в подвале -->
                        <div id="medflexMedtochkaWidgetButton" 
                             data-src="https://booking.medflex.ru/?user=83de76a588b26ad9b729c20d4b1ffbf6"
                             class="d-inline-block"></div> %>
                    </div>
                </div>
            </div>
            
            <hr class="my-3 opacity-25">
            
            <div class="row align-items-center">
                <div class="col-12 col-md-6 mb-2 mb-md-0 text-center text-md-start">
                    <p class="mb-0 text-light opacity-75 small">
                        © <%= Time.now.year %> Клиника доказательной медицины доктора Медведевой
                    </p>
                </div>
                <div class="col-12 col-md-6 text-center text-md-end">
                    <a href="/docs" class="text-light text-decoration-none opacity-75 hover-opacity-100 small me-3">Документы</a>
                    <a href="/privacy" class="text-light text-decoration-none opacity-75 hover-opacity-100 small">Политика конфиденциальности</a>
                </div>
            </div>
        </div>
    </footer>
    <% else %>
        <!-- Для админки - только контент -->
        <%= yield %>
    <% end %>
    
    <!-- Модальное окно для записи -->
    <div class="modal fade" id="appointmentModal" tabindex="-1" aria-labelledby="appointmentModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-lg">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title fs-6 fs-md-5" id="appointmentModalLabel">Запись на прием</h5>
                    <button type="button" class="btn-close touch-target" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <form id="appointmentForm">
                        <input type="hidden" id="appointmentDoctorId" name="doctor_id" value="">
                        
                        <div class="row g-3">
                            <div class="col-12">
                                <label for="patientName" class="form-label small">ФИО *</label>
                                <input type="text" class="form-control form-control-lg" id="patientName" name="patient_name" required>
                            </div>
                            
                            <div class="col-12 col-md-6">
                                <label for="birthDate" class="form-label small">Дата рождения *</label>
                                <input type="date" class="form-control form-control-lg" id="birthDate" name="birth_date" required>
                            </div>
                            
                            <div class="col-12 col-md-6">
                                <label for="phone" class="form-label small">Телефон *</label>
                                <input type="tel" class="form-control form-control-lg" id="phone" name="phone" required>
                            </div>
                            
                            <div class="col-12 col-md-6">
                                <label for="email" class="form-label small">Email *</label>
                                <input type="email" class="form-control form-control-lg" id="email" name="email" required>
                            </div>
                            
                            <div class="col-12 col-md-6">
                                <label for="doctorName" class="form-label small">Врач</label>
                                <input type="text" class="form-control form-control-lg" id="doctorName" readonly>
                            </div>
                            
                            <div class="col-12">
                                <label for="specialtiesSelect" class="form-label small">Специальность врача *</label>
                                <select class="form-select form-select-lg" id="specialtiesSelect" name="specialty_id" required>
                                    <option value="">Выберите специальность</option>
                                </select>
                            </div>
                            
                            <div class="col-12">
                                <label for="message" class="form-label small">Дополнительное сообщение</label>
                                <textarea class="form-control form-control-lg" id="message" name="message" rows="3"></textarea>
                            </div>
                            
                            <div class="col-12">
                                <div class="form-check">
                                    <input class="form-check-input" type="checkbox" id="privacyAccepted" name="privacy_accepted" value="1" required>
                                    <label class="form-check-label small" for="privacyAccepted">
                                        Я соглашаюсь с <a href="/privacy" target="_blank" class="text-decoration-none">политикой конфиденциальности</a> *
                                    </label>
                                </div>
                            </div>
                        </div>
                    </form>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary px-4 touch-target" data-bs-dismiss="modal">Отмена</button>
                    <button type="button" class="btn btn-primary px-4 touch-target" id="submitAppointment">Отправить</button>
                </div>
            </div>
        </div>
    </div>
    
    <!-- Модальное окно для просмотра фотографий -->
    <div class="modal fade" id="imageModal" tabindex="-1" aria-labelledby="imageModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered modal-lg">
            <div class="modal-content bg-dark border-0">
                <div class="modal-body p-0 text-center">
                    <button type="button" class="btn-close btn-close-white position-absolute top-0 end-0 m-3 touch-target" 
                            data-bs-dismiss="modal" 
                            aria-label="Close"></button>
                    <img id="modalImage" src="" class="img-fluid" alt="" loading="lazy">
                </div>
            </div>
        </div>
    </div>
    
    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <!-- PhotoSwipe JS -->
    <script src="https://cdn.jsdelivr.net/npm/photoswipe@5.4.4/dist/photoswipe.umd.min.js"></script>
    
    <!-- Наш JS -->
    <script src="/js/app.js"></script>
    <script src="/js/image-modal.js"></script>
    <script src="/js/reviews.js"></script>
    <script src="/js/contacts.js"></script>
    <script src="/js/doctors.js"></script>
    <script src="/js/prices.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/axios@1.6.7/dist/axios.min.js"></script>
    <script defer src="https://booking.medflex.ru/components/medtochka-button/medtochka-widget-button.js" charset="utf-8"></script>
    
    <!-- JavaScript для выделения активного пункта в выпадающем меню -->
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            const currentUrl = window.location.pathname + window.location.hash;
            const menuItems = document.querySelectorAll('.dropdown-item');
            
            menuItems.forEach(item => {
                item.classList.remove('active');
            });
            
            if (window.location.hash) {
                const hash = window.location.hash;
                menuItems.forEach(item => {
                    if (item.getAttribute('href') === hash || 
                        item.getAttribute('href') === '/about' + hash) {
                        item.classList.add('active');
                    }
                });
            }
            
            if (currentUrl === '/about' || currentUrl === '/about#') {
                const aboutItem = document.querySelector('.dropdown-item[href="/about"]');
                if (aboutItem) {
                    aboutItem.classList.add('active');
                }
            }
            
            // Мобильная навигация
            const navToggler = document.querySelector('.navbar-toggler');
            const navbarCollapse = document.querySelector('.navbar-collapse');
            
            if (navToggler && navbarCollapse) {
                navToggler.addEventListener('click', function() {
                    document.body.classList.toggle('mobile-menu-open');
                });
                
<%#                 // Закрытие меню при клике на ссылку на мобильных
                if (window.innerWidth <= 991) {
                    document.querySelectorAll('.nav-link, .dropdown-item').forEach(link => {
                        link.addEventListener('click', function() {
                            if (navbarCollapse.classList.contains('show')) {
                                navToggler.click();
                            }
                        });
                    });
                }
            } %>
              }          
            // Предотвращение зума на iOS при фокусе на инпутах
            if (/iPad|iPhone|iPod/.test(navigator.userAgent)) {
                document.addEventListener('focus', function(e) {
                    if (e.target.tagName === 'INPUT' || e.target.tagName === 'TEXTAREA' || e.target.tagName === 'SELECT') {
                        document.body.style.fontSize = '16px';
                    }
                }, true);
                
                document.addEventListener('blur', function(e) {
                    document.body.style.fontSize = '';
                }, true);
            }
            
            // Отладочная информация для виджета MedFlex
            console.log('MedFlex widget initialization...');
            
            // Проверка загрузки виджета
            setTimeout(function() {
                if (typeof window.MedFlexWidget === 'undefined') {
                    console.warn('MedFlex widget not loaded. Check the script URL and network tab.');
                    
                    // Создаем резервную кнопку
                    const widgetContainers = document.querySelectorAll('#medflexMedtochkaWidgetButton');
                    widgetContainers.forEach(container => {
                        if (!container.querySelector('button')) {
                            const backupButton = document.createElement('button');
                            backupButton.className = 'btn btn-primary btn-sm px-3 touch-target';
                            backupButton.innerHTML = '<i class="bi bi-person-circle me-1"></i>Личный кабинет';
                            backupButton.onclick = function() {
                                window.open('https://booking.medflex.ru/?user=83de76a588b26ad9b729c20d4b1ffbf6', '_blank');
                            };
                            container.appendChild(backupButton);
                        }
                    });
                } else {
                    console.log('MedFlex widget loaded successfully:', window.MedFlexWidget);
                }
            }, 3000);
        });
    </script>

<script>
    // Фикс для высоты на мобильных устройствах
    function setVH() {
        let vh = window.innerHeight * 0.01;
        document.documentElement.style.setProperty('--vh', `${vh}px`);
    }

    // Установить высоту при загрузке и изменении размера
    setVH();
    window.addEventListener('resize', setVH);
    window.addEventListener('orientationchange', setVH);

    // Оптимизация загрузки изображений для мобильных
    if ('IntersectionObserver' in window) {
        const lazyImages = document.querySelectorAll('img[loading="lazy"]');
        
        const imageObserver = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    const img = entry.target;
                    img.src = img.dataset.src || img.src;
                    img.classList.add('loaded');
                    imageObserver.unobserve(img);
                }
            });
        }, {
            rootMargin: '50px 0px',
            threshold: 0.1
        });

        lazyImages.forEach(img => imageObserver.observe(img));
    }

    // Предотвращение зума на iOS при фокусе
    document.addEventListener('touchstart', function() {}, {passive: true});

<%#     // Оптимизация скролла на iOS
    document.addEventListener('touchmove', function(e) {
        if (e.target.tagName === 'INPUT' || 
            e.target.tagName === 'TEXTAREA' || 
            e.target.tagName === 'SELECT') {
            return;
        }
        e.preventDefault();
    }, { passive: false });
 %>
<%#     // Закрытие меню при клике на ссылку на мобильных
    document.addEventListener('DOMContentLoaded', function() {
        const navbarCollapse = document.querySelector('.navbar-collapse');
        const navLinks = document.querySelectorAll('.nav-link, .dropdown-item');
        
        navLinks.forEach(link => {
            link.addEventListener('click', () => {
                if (window.innerWidth <= 991 && navbarCollapse.classList.contains('show')) {
                    const toggler = document.querySelector('.navbar-toggler');
                    if (toggler) toggler.click();
                }
            });
        });
    }); %>
</script>

<!-- JavaScript для виджета MedFlex -->
<!-- Альтернатива виджету MedFlex -->
<script>
document.addEventListener('DOMContentLoaded', function() {
    // Заменяем контейнеры на iframe
    const widgetContainers = document.querySelectorAll('[id^="medflexMedtochkaWidgetButton"]');
    
    widgetContainers.forEach(container => {
        const isMobile = container.id.includes('Mobile');
        
        // Очищаем контейнер
        container.innerHTML = '';
        
        if (isMobile) {
            // Мобильная версия - кнопка
            const button = document.createElement('a');
            button.href = 'https://booking.medflex.ru/?user=83de76a588b26ad9b729c20d4b1ffbf6';
            button.target = '_blank';
            button.className = 'btn btn-primary w-100 py-3 touch-target';
            button.style.cssText = 'min-height: 48px; display: flex; align-items: center; justify-content: center; font-weight: 500;';
            button.innerHTML = '<i class="bi bi-person-circle me-2"></i>Личный кабинет пациента';
            container.appendChild(button);
        } else {
            // Десктоп версия - кнопка
            const button = document.createElement('a');
            button.href = 'https://booking.medflex.ru/?user=83de76a588b26ad9b729c20d4b1ffbf6';
            button.target = '_blank';
            button.className = 'btn btn-primary btn-sm px-3 touch-target';
            button.style.cssText = 'min-width: 160px; font-weight: 500;';
            button.innerHTML = '<i class="bi bi-person-circle me-1"></i>Личный кабинет';
            container.appendChild(button);
        }
    });
});
</script>
    
</body>
</html>